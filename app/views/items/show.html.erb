<% provide(:title, "商品表示") %>
<p id="notice"><%= notice %></p>
 <table class="attr">

<tr><
  <th>Category:</th>
  <td><%= @item.category %></td>
</tr>

<tr>
  <th>Name:</th>
  <td><%= @item.name %></td>
</tr>

<tr>
  <th>Image:</th>

	<td><%= image_tag @item.image_url %></td>
</tr>

<tr>
  <th>Count:</th>
  <td><%= @item.count %></td>
</tr>

<tr>
  <th>生産者:</th>
  <td><%= link_to @item.member.name, member_path(@item.member.id) %></td>
</tr>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
